---
// src/pages/material/stainless-steel-metal.astro
// Yang terakhir dari seri material!

import Layout from '../../layouts/Layout.astro';
import MaterialDetail from '../../components/MaterialDetail.astro';
import { MATERIALS, SITE_CONFIG } from '../../utils/constants.js';

// 1. Ambil data spesifik untuk Stainless Steel & Metal dari pusat data
const material = MATERIALS.find(m => m.id === 'stainless');

// 2. Jika data tidak ditemukan, alihkan ke halaman 404
if (!material) {
  return Astro.redirect('/404');
}
---

<Layout
  title={`${material.title} | ${SITE_CONFIG.name}`}
  description={material.description}
  keywords={material.keywords}
>
  <MaterialDetail material={material} />
</Layout>